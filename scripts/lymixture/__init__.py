"""
In this subpackage, I put only those functions and things that are necessary for
Julian's EM implementation.
"""
import logging

logger = logging.getLogger(__name__)
logger.addHandler(logging.NullHandler())
